# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
use_frameworks!

target 'ARTestingGround' do
  pod 'ARCL'
  # Pods for ARTestingGround

  target 'ARTestingGroundTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
